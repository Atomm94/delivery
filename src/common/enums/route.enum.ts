export enum Status {
  INCOMING = 'incoming',
  ACTIVE = 'active',
  DONE = 'done',
}

export enum Porter {
  'Without porter' = 1,
  'Take 1 porter with you' = 2,
  'Take 2 porter with you' = 3,
  'Take 3 porter with you' = 4,
}
